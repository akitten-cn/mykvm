# MyKVM Fork 项目级执行规则模板

此文件是待合并模板，不自动覆盖已有 AGENTS。先读取项目原规则，再只加入本项目需要的内容；不修改用户全局 `~/.codex/AGENTS.md`。

## 项目上下文

读取 `docs/handoff/00_交接说明.md` 或工作区 `handoff/00_交接说明.md`，按其导航读取需求、设计、执行计划和源码审查。开发主机是 M4 Mac mini，使用方向仍是 Windows 物理键鼠控制 Mac。

LOL 实机验证不作为开发、合并或打包关卡。没有 Windows/LOL 环境时独立记录待验证，推进 Mac 可执行任务。不得删掉 Windows 实现以让 Mac 构建通过。

## 执行

一次一个任务。先查真实工作树、定位源码，再最小修改与测试。主线程核验真实 diff 和命令退出码后才能进入下一任务。遵守已有的逐任务批准规则，不并行写同一核心文件。

项目实际存在 `$sol-multi-agent-development` 时读取其 SKILL 并按约定使用；不存在时单主线程完成。不得编造已经运行的子代理、未使用的工具、构建结果或实机测试。

每次任务后更新 taskboard、PROGRESS、测试报告与必要的 ADR。未运行不能记 pass。模拟/编译/实机证据严格区分。

## 安全与用户状态

不覆盖用户未提交改动，不强推、不删除无关目录、不默认 `git add .`。秘密、真实设备日志和私人配置留本地。

不引入视频链路、收费运行依赖、驱动、游戏注入、反作弊规避或默认 SYSTEM 服务。不为“修复”关闭 TLS、配对、Gatekeeper、SIP 或防火墙。

默认测试使用 FakeInjector/FakeCapture，不抢真实键鼠。不自动关闭维持用户控制 Mac 的 Deskflow/RustDesk。安装覆盖、授权权限、钥匙串/系统信任变更、公开发布需要用户确认。

## 构建

先核对实际 package scripts、锁文件和平台依赖。Mac 运行适用的 lint/build/cargo lib tests；Windows 条件分支由可用原生 CI检查。Windows/LOL环境缺失不是所有工作阻塞理由，但实际测试失败必须记录并修复相应范围。

保持 fork 独立标识/数据/自启/锁/更新通道；不得默认运行未经审查的上游 sign/install/release 脚本。

## 交付

交付真实修改、提交、测试、产物路径与校验和、风险、未验证清单和回滚方法。代码完成不等于 LOL 已验证；未生成安装包不能编造链接。
